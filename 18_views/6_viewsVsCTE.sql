-- Views Vs CTE
/*
 CTE:
 -- The CTE is used in order to reduce the redundancy within one single query. So it improves the reusability within one query. 
 -- Improve reusability in one queries
 -- Temporary Logic
 
 VIEWS:
 -- We are reducing the redundancies from the multiple queries. So we are reducing the complexity of the whole project.
 -- Improve reusability in multi queries
 -- Persisted logic
 */