-- SQL QUERY CLAUSES
--  Each sql query has usually different sections, different component and we call them clauses
SELECT
FROM
WHERE - That we can use in order to filter our data.
ORDER BY - It will help us to sort our data.