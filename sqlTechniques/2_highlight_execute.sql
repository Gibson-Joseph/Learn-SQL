-- Highlight and Execute
SELECT *
from customers
WHERE country = 'Germany';
/*
 If you have query like this, you are selecting from the table filtering the data and now you would like not to execute the whole thing. You would like the execute the part of this query. 
 */
/*
 When you’re writing multiple SQL statements in the same editor window, you can highlight (select) a specific query with your mouse or keyboard — and then execute only that selected part, instead of running the entire file.
 */