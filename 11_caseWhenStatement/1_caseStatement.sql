-- CASE Statement
-- Evaluates a list of conditions and returns a value when the first condition is met.
/*
 It starts with the keyword CASE. This case indicates now we are starting a logic a coinditional logic in SQL. It's like programming laguagtes as you start with the ifelse.
 And the whole logi as well ends with another keyword called END. So once SQL sees the end, so this is sht end of the conditional logic.
 
 So now what we're going to have in betweeen is the conditional logics. 
 
 So the conditional logic starts with the keyword WHEN. Now we are telling the SQL we have a condition to be evaluated.
 
 So now we have to tell SQL what can happen if this condition is fulfilled. So now we have to use another keyword called THEN. So now we are telling SQL show this resutls if the condition is true.
 
 And now of course we can go and add a second condition inside our case statement.
 
 And of course it's very important to understand the syntax of that. SQL can go and process the conditions from the top to the bottom.
 
 So the order of the conditions is very important in your logic.
 
 And now of course we can go and add multiple conditions depend on the logic using the keyword WHEN.
 
 Once you are done defining all the condtions, we can go and specify and ELSE keyword. The ELSE can introduce the default value and it is optional.
 */