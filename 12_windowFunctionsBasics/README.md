# Window Functions Basics

## Aggregate Functions

- COUNT(expr)
- SUM(expr)
- AVG(expr)
- MIN(expr)
- MAX(expr)

## Rank Functions

- ROW_NUMBER()
- RANK()
- DENSE_RANK()
- CUME_DIST()
- PERCENT_RANK()
- NTILE(n)

## Value(Analytics) Functions

- LEAD(expr, offset, default)
- LAG(expr, offset, default)
- FIRST_VALUE(expr)
-
